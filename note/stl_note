//array of STL:- 


//Vector<Integer> vec = new Vector<>();
//Vector:- dynamic:- 
//Java Vector Class - Built-in Methods and Attributes
/*Attributes
size() → Returns the number of elements.

capacity() → Returns the current capacity of the vector.

isEmpty() → Checks if the vector is empty.

firstElement() → Returns the first element.

lastElement() → Returns the last element.

Modification Methods
add(E e) → Adds an element to the end.

add(int index, E element) → Adds an element at a specific index.

remove(Object o) → Removes the first occurrence of an element.

remove(int index) → Removes an element at a specific index.

clear() → Removes all elements.

set(int index, E element) → Updates an element at a specific index.

insertElementAt(E obj, int index) → Inserts an element at a specific index.

removeElementAt(int index) → Removes an element at a specific index.

**Double-Ended Queues (Deque)** can be implemented using the Deque interface, which is available in the java.util package. it is dynamic also
*/
//Deque<Integer> deque = new ArrayDeque<>();