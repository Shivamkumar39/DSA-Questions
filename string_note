charAt()= variablename.charAt(index_number)
length()== find length();

indexof()== finding charecter in which index available
compareTo() == compare two string cheak all area like which careecter bige behaf of asci value

BitwiseOperaters
&&== and
||== or
^== xor(both must not be 1 it must be any one contion true only)
~== not
<<== left shift
>>== right shift
>>>== zero fill right shift

formula for finding digit in number
int count=0;
while(number!=0)
{
number=number/10;
count++;
}  
System.out.println("number of digit in number is:"+count);

finding bit in number
int count=0;
while(number!=0)
{
    ans =(ans * 10^i) + digit;
number=number>>1;
count++;
}
System.out.println("number of bit in number is:"+count);


//switch & function
How to out from switch case in infinite loop?
continue is not valid with switch case

